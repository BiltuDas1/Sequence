# UI Testing (Maestro)

Tests are located in `.maestro/`.

## Commands
- **Run all**: `maestro test .maestro/`
- **Run flow**: `maestro test .maestro/login/login_flow.yaml`
- **Debug**: `maestro studio`

## Usage
Add `.yaml` flows for new features. Use `testTag` for Compose components where text selection is ambiguous.
